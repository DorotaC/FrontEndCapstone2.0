import { generateAndPost } from '../src/client/js/app'

describe("generateAndPost function checker", () => {
  test("This test should check if function generateAndPost exists", () => {
    expect(generateAndPost).toBeDefined()
  })
})
